spring:
  application:
    name: jme-object-storage-test
  main:
    banner-mode: off
  docker:
    compose:
      enabled: true
      file: ./docker/docker-compose.yml
      lifecycle-management: start-and-stop
      skip:
        in-tests: false
      arguments:
        - "--project-name=${COMPOSE_PROJECT_NAME:jme-object-storage-test}"
      readiness:
        timeout: 120s
